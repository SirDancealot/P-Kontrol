<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context=".UI.Fragments.WriteTipFragment.WriteTipInternalFragments.WriteTip_State_WriteText"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Thanks for Contributing -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="75dp"
        android:orientation="vertical"
        android:background="@drawable/writetip_navbar_top_shape"

        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        >

        <!-- Title  -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="10dp"

            android:fontFamily="@font/ubuntu_regular"
            android:gravity="center"
            android:text="submit Contribution"
            android:textColor="@color/color_matBlack"
            android:textSize="24sp"

            />
        <!-- Submit Button -->
        <Button
            android:id="@+id/WriteTip_SubmitButton"
            android:layout_width="250dp"
            android:layout_height="50dp"
            android:background="@color/colorHighlightSecond"
            android:layout_gravity="center"
            android:text="submit"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>